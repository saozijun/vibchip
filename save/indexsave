<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Vibchip - 领航芯片技术</title>

  <style>
    :root {
      --primary-blue: #0066ff;
      --tech-cyan: #00f2ff;
      --bright-bg: #f4f7fa;
      --dark-panel: #0a1628;
      --glass-white: rgba(255, 255, 255, 0.8);
      --text-dark: #1a2b3c;
      --text-gray: #5a6b7d;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background-color: var(--bright-bg);
      color: var(--text-dark);
      font-family: 'PingFang SC', 'Microsoft YaHei', sans-serif;
      line-height: 1.6;
      overflow-x: hidden;
    }

    .container {
      width: 90%;
      max-width: 1200px;
      margin: 0 auto;
    }


    .carousel-wrapper {
      position: relative;
      height: 650px;
      width: 100vw;
      left: 50%;
      right: 50%;
      margin-left: -50vw;
      margin-right: -50vw;
      overflow: hidden;
      background: #000;
    }

    .carousel-track {
      display: flex;
      height: 100%;
      transition: transform 0.8s cubic-bezier(0.65, 0, 0.35, 1);
    }

    .slide {
      min-width: 100%;
      height: 100%;
      display: flex;
      justify-content: center;
      align-items: center;
      background-size: cover;
      background-position: center;
      position: relative;
    }

    .slide::after {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: linear-gradient(45deg, rgba(0, 102, 255, 0.3), rgba(0, 0, 0, 0.5));
    }

    .slide-content {
      position: relative;
      z-index: 2;
      font-size: 4.5rem;
      font-weight: 900;
      color: #fff;
      text-align: center;
      letter-spacing: 8px;
      text-transform: uppercase;
      text-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
    }

    .carousel-btn {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      background: rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(5px);
      border: 1px solid rgba(255, 255, 255, 0.4);
      color: #fff;
      width: 55px;
      height: 55px;
      cursor: pointer;
      z-index: 20;
      border-radius: 50%;
      font-size: 1.5rem;
      transition: 0.4s;
    }

    .carousel-btn:hover {
      background: var(--tech-cyan);
      border-color: var(--tech-cyan);
      box-shadow: 0 0 25px var(--tech-cyan);
      transform: translateY(-50%) scale(1.1);
    }

    .prev {
      left: 40px;
    }

    .next {
      right: 40px;
    }

    .carousel-dots {
      position: absolute;
      bottom: 40px;
      left: 50%;
      transform: translateX(-50%);
      display: flex;
      gap: 12px;
      z-index: 20;
    }

    .dot {
      width: 30px;
      height: 5px;
      background: rgba(255, 255, 255, 0.3);
      border-radius: 10px;
      cursor: pointer;
      transition: 0.4s;
    }

    .dot.active {
      background: var(--tech-cyan);
      width: 50px;
      box-shadow: 0 0 15px var(--tech-cyan);
    }




    .feature-section {
      width: 100%;
      padding: 100px 0;
      background-color: #fff;
    }


    .feature-section:nth-child(odd) {
      background-color: #f9fbff;
    }

    .feature-row {
      display: flex;
      align-items: center;
      gap: 80px;
    }


    .feature-section:nth-child(even) .feature-row {
      flex-direction: row-reverse;
    }

    .feature-image {
      flex: 1;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 20px 40px rgba(0, 102, 255, 0.1);
    }

    .feature-image img {
      width: 100%;
      height: auto;
      display: block;
      transition: transform 0.5s ease;
    }

    .feature-image:hover img {
      transform: scale(1.05);
    }

    .feature-content {
      flex: 1;
    }

    .feature-content .lev {
      color: #0f4caa;
      font-weight: 520;
      font-size: 30px;
      position: relative;
      top: 30px;
    }

    .feature-content h2 {
      font-size: 2.4rem;
      color: var(--dark-panel);
      margin-bottom: 25px;
      position: relative;
    }

    .feature-content h2::after {
      content: '';
      position: absolute;
      left: 0;
      bottom: -10px;
      width: 50px;
      height: 4px;
      background: var(--primary-blue);
    }

    .feature-content p {
      font-size: 1.1rem;
      color: var(--text-gray);
      margin-bottom: 30px;
      line-height: 1.8;
    }

    .feature-list {
      list-style: none;
    }

    .feature-list li {
      margin-bottom: 15px;
      display: flex;
      align-items: center;
      color: var(--text-dark);
      font-weight: 500;
    }

    .feature-list li::before {
      content: '✓';
      margin-right: 15px;
      color: var(--primary-blue);
      font-weight: bold;
      background: rgba(0, 102, 255, 0.1);
      width: 25px;
      height: 25px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      font-size: 14px;
    }

    @media (max-width: 992px) {

      .feature-row,
      .feature-section:nth-child(even) .feature-row {
        flex-direction: column;
        gap: 40px;
      }

      .feature-image,
      .feature-content {
        width: 100%;
      }
    }






    .feature-grey {
      width: 100%;
      padding: 100px 0;
      background-color: #f4f7fa;
    }

    .feature-grey .feature-row {
      display: flex;
      flex-direction: row-reverse;
      align-items: center;
      gap: 80px;
    }


    .feature-grey .feature-image {
      flex: 1;
      border-radius: 24px;
      overflow: hidden;
      box-shadow: 0 20px 40px rgba(0, 0, 0, 0.05);
    }

    .feature-grey .feature-image img {
      width: 100%;
      display: block;
    }

    .feature-grey .feature-content {
      flex: 1;
    }

    .feature-grey .feature-content h2 {
      font-size: 2.4rem;
      color: var(--dark-panel);
      margin-bottom: 25px;
    }


    .check-list {
      list-style: none;
      margin-top: 30px;
    }

    .check-list li {
      display: flex;
      align-items: flex-start;
      margin-bottom: 20px;
      color: var(--text-dark);
      font-size: 1rem;
    }

    .check-list li::before {
      content: '✔';
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 20px;
      height: 20px;
      border: 2px solid var(--primary-blue);
      border-radius: 4px;

      margin-right: 15px;
      color: var(--primary-blue);
      font-size: 12px;
      font-weight: bold;
      flex-shrink: 0;
      margin-top: 4px;
    }




    .intro-section {
      width: 92%;
      max-width: 1300px;
      margin: 80px auto;
      padding: 80px;
      background: #fff;
      border-radius: 40px;
      box-shadow: 0 20px 50px rgba(0, 0, 0, 0.04);

      display: flex;
      align-items: center;
      gap: 60px;
    }

    .intro-image {
      flex: 0 0 420px;
      position: relative;
    }

    .intro-image img {
      width: 100%;
      height: 500px;
      object-fit: cover;
      border-radius: 20px;
      display: block;
      position: relative;
      z-index: 1;
    }

    .intro-text {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    .intro-text p {
      font-size: 1.05rem;
      color: var(--text-gray);
      margin-bottom: 1.2rem;
      line-height: 1.8;
      text-align: justify;
    }

    .intro-text p:first-child {
      font-size: 1.25rem;
      color: var(--text-dark);
      font-weight: 600;
      line-height: 1.6;
      margin-bottom: 2rem;
    }


    @media (max-width: 1100px) {
      .intro-section {
        padding: 40px;
        gap: 40px;
      }

      .intro-image {
        flex: 0 0 350px;
      }
    }

    @media (max-width: 992px) {
      .intro-section {
        flex-direction: column;
        text-align: center;
      }

      .intro-image {
        flex: 0 0 auto;
        width: 100%;
        max-width: 500px;
      }

      .intro-image img {
        height: 300px;

      }

      .intro-text p {
        text-align: center;
      }
    }


    .site-footer {
      background: var(--dark-panel);
      padding: 80px 0 50px;
      color: #fff;
    }

    .footer-flex {
      display: flex;
      justify-content: space-between;
    }

    .footer-logo img {
      height: 150px;
    }

    .footer-links {
      display: flex;
      gap: 100px;
    }

    .link-group h4 {
      color: var(--tech-cyan);
      margin-bottom: 25px;
      font-size: 1.1rem;
      text-transform: uppercase;
    }

    .link-group ul {
      list-style: none;
    }

    .link-group ul li {
      margin-bottom: 12px;
    }

    .link-group ul li a {
      color: #a0aec0;
      text-decoration: none;
      transition: 0.3s;
    }

    .link-group ul li a:hover {
      color: #fff;
    }


    @media (max-width: 992px) {
      .intro-section {
        flex-direction: column;
        padding: 40px;
        margin-top: 20px;
      }

      .slide-content {
        font-size: 2.5rem;
      }

      .footer-flex {
        flex-direction: column;
        gap: 50px;
        align-items: center;
        text-align: center;
      }

      .footer-links {
        gap: 50px;
      }
    }
  </style>
</head>

<body>
  <div class="layout-wrapper">

    <header id="header-placeholder" class="site-header"></header>

    <main class="site-content">
      <div class="carousel-wrapper">
        <div class="carousel-track" id="carouselTrack">
          <div class="slide"
            style="background-image: url('https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&q=80&w=1920')">
            <!-- <div class="slide-content">高性能处理器</div> -->
          </div>
          <div class="slide"
            style="background-image: url('https://images.unsplash.com/photo-1550751827-4bd374c3f58b?auto=format&fit=crop&q=80&w=1920')">
            <!-- <div class="slide-content">极速数据传输</div> -->
          </div>
          <div class="slide" style="background-image: url('img2/about3.png')">
            <!-- <div class="slide-content">工业级稳定性</div> -->
          </div>
        </div>
        <button class="carousel-btn prev" id="prevBtn">&#10094;</button>
        <button class="carousel-btn next" id="nextBtn">&#10095;</button>
        <div class="carousel-dots" id="carouselDots"></div>
      </div>


      <section class="intro-section">
        <div class="intro-image">
          <img src="https://images.unsplash.com/photo-1581091226825-a6a2a5aee158?auto=format&fit=crop&q=80&w=800"
            alt="Laboratory">
        </div>
        <div class="intro-text">
          <p>Vibchip is a well-known and established electronic component distributor with a dynamic sales team and a
            senior purchasing team with over 20 years of experience.</p>

          <p>We maintain a sufficient "safety stock" to ensure you receive reasonable prices and reliable component
            quotations on the same day. Whether urgently seeking a specific component or managing an entire supply
            chain, we are your reliable "add-on" partner.</p>

          <p>We are dedicated to problem-solving and empowering clients to create greater value. We firmly believe that
            business is built on people; when we deliver exceptional service, rewards follow naturally.</p>

          <p>By combining sound procurement strategies with industry expertise, Vibchip empowers clients to source
            high-reliability components with lower risks and optimized costs. Every component undergoes rigorous
            electrical testing prior to shipment to ensure uncompromising quality.</p>
        </div>
      </section>



      <section class="feature-section">
        <div class="container feature-row">
          <div class="feature-image">
            <img src="img2/service1.png" alt="短缺物料采购">
          </div>
          <div class="feature-content">
            <p class="lev">lv1</p>
            <h2>Shortage Part Procurement</h2>
            <p>Develop a scientific procurement strategy in response to procurement changes caused by supply-demand
              relationships；Seize the initiative in procurement ahead of shortage.</p>
            <ul class="feature-list">
              <li>Powerful data capabilities to quickly identify component shortages</li>
              <li>Acute analytical skills to be ahead of market trends in component shortages</li>
              <li>Intelligent system to provide real-time access to global supply channel inventory</li>
            </ul>
          </div>
        </div>
      </section>


      <section class="feature-grey">
        <div class="container feature-row">
          <div class="feature-image">
            <img src="img2/service2.png" alt="降低成本策略"
              onerror="this.src='https://images.unsplash.com/photo-1554224155-6726b3ff858f?auto=format&fit=crop&q=80&w=800'">
          </div>

          <div class="feature-content">
            <p class="lev">lv2</p>
            <h2>PPV Cost-down Strategy</h2>
            <p>Continuously identify cost-reduction opportunities, align with your target price, and reduce
              intermediate
              cost in your procurement process.</p>

            <ul class="check-list">
              <li>Equippe with an intelligent data system for real-time component prices updating</li>
              <li>Integrate with multiple major distributors for direct access to the best prices</li>
              <li>A professional procurement team conducts global sourcing to customize electronic component
                procurement
                solutions for you.</li>
            </ul>
          </div>
        </div>
      </section>


      <section class="feature-section">
        <div class="container feature-row">
          <div class="feature-image">
            <img src="img2/service3.png" alt="短缺物料采购">
          </div>
          <div class="feature-content">
            <p class="lev">lv3</p>
            <h2>Integrated Supplier Service</h2>
            <p>Establish a global network and integrate high-quality suppliers to alleviate your procurement burden
              while ensuring quality and supply reliability</p>
            <ul class="feature-list">
              <li>Maintain close collaboration with global suppliers and implement tiered management to rigorously
                control supplier quality</li>
              <li>Cover comprehensively over 10000+ electronic component suppliers and over 10 millions of SKUs</li>
              <li>Respond to customer needs flexibly and efficiently based on stable supplier relationships and a
                robust
                management system</li>
            </ul>
          </div>
        </div>
      </section>


      <section class="feature-grey">
        <div class="container feature-row">
          <div class="feature-image">
            <img src="img2/service4.png" alt="降低成本策略"
              onerror="this.src='https://images.unsplash.com/photo-1554224155-6726b3ff858f?auto=format&fit=crop&q=80&w=800'">
          </div>

          <div class="feature-content">
            <p class="lev">lv4</p>
            <h2>Excess Solutions</h2>
            <p>Based on your inventory status, we leverage big data analytics and professional team insights to
              analyze
              market demand, implement differentiated pricing for your materials, and execute multi-channel promotion
              strategies to formulate the optimal solution for you.</p>

            <ul class="check-list">
              <li>A dedicated professional team provides personalized services to formulate the optimal inventory
                management solution</li>
              <li>Turn your inventory into revenue, offer inventory consignment and online showcase to your excess
                inventory</li>
            </ul>
          </div>
        </div>
      </section>


    </main>

    <footer id="footer-placeholder" class="site-footer"></footer>

  </div>

  <script>

    // 动态加载 Header 和 Footer
    // 加载外部 HTML 组件的函数
    async function loadComponent(id, url) {
      try {
        const response = await fetch(url);
        const data = await response.text();
        document.getElementById(id).innerHTML = data;

        // 如果是加载 Header，执行高亮逻辑
        if (id === 'header-placeholder') highlightNav();
      } catch (err) {
        console.error('组件加载失败:', err);
      }
    }

    function highlightNav() {
      const currentPath = window.location.pathname.split("/").pop() || "index.html";
      const navLinks = document.querySelectorAll('.main-nav a');
      navLinks.forEach(link => {
        if (link.getAttribute('href') === currentPath) {
          link.style.color = "#00f2ff";
        }
      });
    }

    // 初始化加载
    window.onload = () => {
      loadComponent('header-placeholder', 'header.html');
      loadComponent('footer-placeholder', 'footer.html');
    };



    document.addEventListener('DOMContentLoaded', function () {
      const track = document.getElementById('carouselTrack');
      const slides = Array.from(track.children);
      const dotsContainer = document.getElementById('carouselDots');
      const prevBtn = document.getElementById('prevBtn');
      const nextBtn = document.getElementById('nextBtn');

      let currentIndex = 0;
      const autoTime = 5000; // 建议改为5000ms(5秒)，10秒略长
      let timer = null;

      // 初始化圆点
      slides.forEach((_, i) => {
        const dot = document.createElement('div');
        dot.classList.add('dot');
        if (i === 0) dot.classList.add('active');
        dot.onclick = () => {
          goTo(i);
          resetTimer();
        };
        dotsContainer.appendChild(dot);
      });
      const dots = Array.from(dotsContainer.children);

      // 核心切换函数
      function goTo(index) {
        if (index < 0) index = slides.length - 1;
        if (index >= slides.length) index = 0;
        currentIndex = index;
        track.style.transform = `translateX(-${currentIndex * 100}%)`;
        dots.forEach((dot, i) => {
          dot.classList.toggle('active', i === currentIndex);
        });
      }

      // 修改：手动控制不使用 click() 触发，而是直接调用函数
      nextBtn.onclick = () => {
        goTo(currentIndex + 1);
        resetTimer();
      };

      prevBtn.onclick = () => {
        goTo(currentIndex - 1);
        resetTimer();
      };

      // 修改：自动轮播直接调用 goTo，避免触发 nextBtn 上的 resetTimer
      function startTimer() {
        if (timer) clearInterval(timer);
        timer = setInterval(() => {
          goTo(currentIndex + 1);
        }, autoTime);
      }

      function resetTimer() {
        clearInterval(timer);
        startTimer();
      }

      // 启动
      startTimer();

      // 鼠标交互控制
      const wrapper = document.querySelector('.carousel-wrapper');
      wrapper.addEventListener('mouseenter', () => clearInterval(timer));
      wrapper.addEventListener('mouseleave', startTimer);
    });
  </script>
</body>

</html>